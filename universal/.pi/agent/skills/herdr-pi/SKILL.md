---
name: herdr-pi
description: Pi entrypoint for terminal work owned in Herdr. Use the shared Herdr workflow and current global delegation policy.
---

# Herdr from Pi

Read the canonical shared `~/.agents/skills/herdr/SKILL.md` for commands, ownership, sending, and waiting. Follow the global originating-app and model policy. Herdr and worktrees are optional for creative work; worktree steps apply only to actual Git writing slices that need isolation. Do not maintain a second set of launch, review, or model defaults here.
